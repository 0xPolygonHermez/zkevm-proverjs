include "global.pil";


namespace KeccakF(%N);

    pol constant ConnA, ConnB, ConnC, GateType, kGateType, kA, kB, kC;
    pol commit a,b,c;

    {a, b, c} connect {ConnA, ConnB, ConnC};

    { GateType, a, b, c } in { kGateType, kA, kB, kC } ;

    Global.L1 * a = 0;
    Global.L1 * (72624976668147841-b) = 0;